<div class="container">
    <div class="row">

        <div class="sec-title text-center">
            <h2 id="collectionId">
                <%- code %>
            </h2>
            <div class="devider">
                <i class="far fa-heart"></i>
            </div>
        </div>

        <div class="sec-sub-title text-center">

            <% if(isShared){ %>
                <p style="color: #65C18C;">Được chia sẻ với bạn.</p>
                <% }else{ %>
                    <p style="color: #FC4F4F;">Chế độ chia sẻ đang tắt!</p>
                    <%} %>

        </div>
    </div>
</div>


<% if(isShared){ %>
    <div class="project-wrapper">

        <% images.forEach(item=> { %>
            <figure class="mix work-item" id="item_<%- item._id %>">
                <img src="<%- item.url %>" alt="">
                <figcaption class="overlay">
                    <a class="fancybox" rel="works" title="<%- item.title %>" href="<%- item.url %>">
                        <i class="fas fa-eye"></i>
                    </a>
                    <h4 id="title_<%- item._id %>">
                        <%- item.title %>
                    </h4>
                    <p>
                        <%- item.url %>
                    </p>
                </figcaption>
            </figure>
            <% }) %>
    </div>
    <%} %>